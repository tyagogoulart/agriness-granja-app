type MetricsTypes = {
  smallMargin: number;
  baseMargin: number;
  doubleBaseMargin: number;
  baseRadius: number;
};

const metrics: MetricsTypes = {
  smallMargin: 8,
  baseMargin: 16,
  doubleBaseMargin: 32,
  baseRadius: 4,
};

export default metrics;
